<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="fauzi.hilmy.app5revisi">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AboutApp"
            android:label="About Arbain Nawawi App"/>
        <activity android:name=".HomeActivity" />
        <activity android:name=".ActivityAbout" />
        <activity
            android:name=".AboutNawawi"
            android:label="Tentang Imam An-Nawawi" />
        <activity android:name=".AboutDeveloper"
            android:label="About Developer"/>
        <activity android:name=".ActivityDetailHadis" />
        <activity
            android:name=".DetailHadisScrolling"
            android:theme="@style/AppTheme.NoActionBar"
            android:parentActivityName=".MainActivity"/>
    </application>

</manifest>